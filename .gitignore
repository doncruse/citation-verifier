.env
.claude/settings.local.json
.claude/projects/
__pycache__/
*.pyc
*.egg-info/
venv/
.venv/
.DS_Store
.citation_cache.json

# Ephemeral diagnostic output (timestamped extraction/verification runs)
tests/data/results/citations_extracted_2*.json
tests/data/results/verification_*.json

# Large PDFs — synced separately
scratch/hallucination_opinions/

# Web app smoke-test log output (regenerable, file-locked on Windows)
scratch/app_stderr.log
scratch/app_stdout.log

# Pilot A opinion-text caches (regenerable, ~5-25 MB each)
benchmark/pilot_a/cited_opinion_cache/
benchmark/pilot_a/dcd_citing_opinion_cache/

# Pilot A run logs (chatty eyecite warnings, regenerable)
benchmark/pilot_a/_*.txt
benchmark/pilot_a/_smoke_*.csv

# Private working notes (not for public repo)
docs/mcp-feedback.md

# Worktrees
.worktrees/
.claude/worktrees/

# Claude Code harness lock file (machine-local)
.claude/scheduled_tasks.lock

# Replit auto-generated files
.upm/
.cache/
.config/

# Benchmark v1 — citing-court opinion cache (regenerable)
benchmark/releases/v1/citing_opinion_cache/

# Benchmark v1 — chatty run logs (regenerable; keep CSVs and markdown)
benchmark/releases/v1/_*.txt

# A/B harness live-run working dirs + loose score files (evidence kept in scratch/ab_runs/)
tests/data/results/ab_runs/
tests/data/results/ab_*.jsonl
